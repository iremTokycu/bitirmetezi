package com.taxi.core.driverapp.history;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DriverHistoryEntityRepository extends JpaRepository<DriverHistoryEntity, Long> {
}
